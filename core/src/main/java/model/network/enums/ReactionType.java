package model.network.enums;

public enum ReactionType {
    EMOJI,           // Preset emoji identifier (e.g. SMILE, CRY, ANGRY, SUNFLOWER)
    TEXT,            // Quick text message / chat snippet
    TAUNT,           // Tactical sound or visual taunt (e.g. "BRAINS!", "NO_WAY")
    SURRENDER        // Surrender forfeit signal
}
