package model.enums;

public enum PlacableLayer {
    GROUND, // lily pads, graves, ice blocks
    MAIN, // plants
    OVERLAY, // plants or anything that can be stacked on the underlying plant
}
